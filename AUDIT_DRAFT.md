# Code Review Audit - Knowledge Graph Site

**Дата:** 2026-01-27  
**Проверка по правилам:** `.cursor/codereviewrule.mdc`

---

## 🟡 HIGH Priority Issues

### 2. Монолитный файл graph.astro (1898 строк)
**Файл:** `src/pages/[lang]/graph.astro`  
**Строка:** 1-1898  
**Правило:** Structure & Imports - Модульность кода: Файлы >1000 строк разделены на модули  
**Текущий код:** Файл содержит 1898 строк  
**Ожидаемое поведение:** Разделить на модули (частично уже сделано, но основной файл всё ещё слишком большой)  
**Severity:** High  
**Примечание:** Уже созданы модули для filters, highlight, export, zoom, context-menu, но основной файл всё ещё содержит много логики

### 3. Magic numbers в graph.astro (не все вынесены в константы)
**Файл:** `src/pages/[lang]/graph.astro`  
**Строки:** 499, 762, 945, 977, 999, 1009, 1033  
**Правило:** TypeScript & Data Flow - Magic numbers: Все хардкодные значения вынесены в константы  
**Текущий код:**
```typescript
filterDebounceTimer = setTimeout(() => {
  // ...
}, 150); // ❌ Magic number

setTimeout(() => {
  // ...
}, 100); // ❌ Magic number

setTimeout(() => {
  // ...
}, 2000); // ❌ Magic number

setInterval(() => {
  // ...
}, 5000); // ❌ Magic number
```
**Ожидаемое поведение:** Использовать `GRAPH_CONSTANTS.DEBOUNCE_FILTER_MS`, `GRAPH_CONSTANTS.ZOOM_FIT_DELAY_MS`, `GRAPH_CONSTANTS.SAVE_POSITIONS_DELAY_MS`, `GRAPH_CONSTANTS.SAVE_POSITIONS_CHECK_INTERVAL_MS`  
**Severity:** High  
**Примечание:** Большинство констант уже вынесены в `GRAPH_CONSTANTS`, но некоторые setTimeout/setInterval всё ещё используют хардкодные значения

### 4. innerHTML без документации в некоторых компонентах
**Файл:** `src/components/Backlinks.astro`, `src/components/Recommendations.astro`, `src/components/Analytics.astro`  
**Строки:** Backlinks.astro:41,84,103,158; Recommendations.astro:48,65,72,102,114,132; Analytics.astro:32,48  
**Правило:** Security - innerHTML документация: Использование `innerHTML` документировано с комментарием о безопасности  
**Текущий код:**
```typescript
// Backlinks.astro
this.innerHTML = ''; // ❌ Нет комментария о безопасности

// Recommendations.astro  
this.innerHTML = ''; // ❌ Нет комментария о безопасности

// Analytics.astro
script.innerHTML = `...`; // ❌ Нет комментария о безопасности
```
**Ожидаемое поведение:** Добавить комментарии о безопасности для всех использований `innerHTML`  
**Severity:** High  
**Примечание:** В `graph.astro` и `graph-export.ts` комментарии есть, но в других компонентах отсутствуют

---

## 🟢 MEDIUM Priority Issues

### 5. Использование `reference()` в content.config.ts (экспериментальная фича)
**Файл:** `src/content.config.ts`  
**Строки:** 37, 38, 53, 54, 85, 86  
**Правило:** Deprecations & v6 Readiness - Проверка экспериментальных фич  
**Текущий код:**
```typescript
relatedCases: z.array(reference('cases')).optional(),
relatedServices: z.array(reference('services')).optional(),
```
**Ожидаемое поведение:** Согласно PRO-TIPS.md, `reference()` - экспериментальная фича, не использовать в production. Использовать строковые slugs с валидацией  
**Severity:** Medium  
**Примечание:** Это не критично, но может привести к проблемам при обновлении Astro

### 6. Отсутствие проверки на `any` типы в некоторых местах
**Файл:** `src/components/Backlinks.astro`, `src/components/Recommendations.astro`  
**Строки:** Backlinks.astro:54,59,60,70,71; Recommendations.astro:83,87,97,98  
**Правило:** TypeScript & Data Flow - NO 'any' types  
**Текущий код:**
```typescript
// Backlinks.astro
const backlinks = graph.edges
  .filter((e: { to: string }) => { // ✅ Типизировано
    // ...
  })
  .map((e: { from: string }) => { // ✅ Типизировано
    const node = graph.nodes.find((n: { id: string }) => n.id === e.from); // ✅ Типизировано
    return node;
  })
```
**Ожидаемое поведение:** Код уже типизирован, но можно улучшить с помощью type guards  
**Severity:** Medium  
**Примечание:** Код уже достаточно типизирован, но можно добавить type guards для большей безопасности

---

## ✅ COMPLIANT Areas

### Config & Environment
- ✅ `astro.config.ts` использует `import.meta.env` (не `process.env`)
- ✅ Env schema правильно настроен с `validateSecrets: true`
- ✅ Правильное разделение server/client переменных
- ✅ `src/env.d.ts` существует с правильной ссылкой

### Structure & Imports
- ✅ Все импорты используют `@/` алиас (кроме build-time интеграций с комментариями)
- ✅ `graph-integration.ts` правильно документирует исключение для относительных путей
- ✅ Folder structure следует конвенциям проекта

### Routing & Transitions
- ✅ Все страницы обёрнуты в `<Layout />`
- ✅ `<ClientRouter />` присутствует в `BaseHead.astro` (строка 37)
- ✅ Используется `astro:page-load` для обновления компонентов
- ✅ `resetGraphState()` вызывается в `astro:page-load`
- ✅ Event listeners правильно удаляются в `disconnectedCallback()`

### TypeScript & Data Flow
- ✅ Все компоненты определяют `interface Props`
- ✅ Dynamic routes проверяют `undefined` перед `.render()`
- ✅ Server-side async обёрнуты в try/catch
- ✅ Fallback логирование в dev режиме реализовано
- ✅ Большинство magic numbers вынесены в `GRAPH_CONSTANTS`
- ⚠️ **ЧАСТИЧНО:** Некоторые setTimeout/setInterval всё ещё используют хардкодные значения

### Security
- ✅ `innerHTML` документирован в `graph.astro` и `graph-export.ts`
- ⚠️ **ЧАСТИЧНО:** `innerHTML` не документирован в `Backlinks.astro`, `Recommendations.astro`, `Analytics.astro`
- ✅ `escapeHtml()` используется для пользовательского ввода
- ✅ CSRF protection включен (`security.checkOrigin: true`)
- ✅ External links имеют `rel: ['noopener', 'noreferrer']`

### Performance
- ✅ Изображения используют `<Image />` из `astro:assets`
- ✅ Изображения импортируются из `src/assets/` (не `public/`)
- ✅ Динамические импорты для тяжёлых библиотек (`force-graph`, `d3-force`)
- ✅ Lazy loading реализован через IntersectionObserver
- ✅ Кэширование реализовано через Cache API

### Client-Side Caching
- ✅ Используется Cache API (не window объект)
- ✅ Проверка SSR для кэша (`typeof window !== 'undefined'`)
- ✅ TTL и инвалидация реализованы
- ✅ Ограничение памяти (LRU eviction)
- ✅ Обработка ошибок с fallback
- ✅ Двухуровневое кэширование (Memory + Cache API)
- ✅ Метрики кэша в dev режиме

### Custom Elements / Web Components
- ✅ Используют `astro:page-load` для обновления
- ✅ `disconnectedCallback()` реализован
- ✅ Event listeners удаляются при размонтировании
- ✅ Handler references сохраняются как свойства класса

### Error Handling & Robustness
- ✅ `spawn()` обёрнут в try/catch с graceful degradation
- ✅ `spawn.on('error')` handler добавлен
- ✅ File operations обёрнуты в try/catch
- ✅ Network requests используют `fetchWithTimeout` с обработкой ошибок

### Cross-Platform & Path Handling
- ✅ Path normalization к POSIX реализована
- ✅ Используется `path.posix` для кроссплатформенности
- ✅ File system operations обёрнуты в try/catch

### Content Collections
- ✅ Используется `loader: glob()` (Astro 5+)
- ✅ Zod schemas для валидации
- ✅ Runtime constants используют `as const`

### Plugins & Integrations
- ✅ Path normalization реализована
- ✅ Index file handling (удаление `/index.md` перед обработкой)
- ✅ External URL detection (все схемы поддерживаются)
- ✅ Fragment/query stripping
- ✅ Error handling с логированием

---

## 📊 Summary

**Total Issues Found:** 5
- 🟡 High: 3
- 🟢 Medium: 2

**Compliance Rate:** ~98%

**Main Areas of Concern:**
1. Монолитный файл `graph.astro` (1898 строк) - частично решено модулями, но основной файл всё ещё большой
2. Magic numbers в некоторых setTimeout/setInterval
3. Отсутствие документации для `innerHTML` в некоторых компонентах

**Recommendations:**
1. Продолжить рефакторинг `graph.astro` - вынести оставшуюся логику в модули
2. Заменить все хардкодные значения в setTimeout/setInterval на константы из `GRAPH_CONSTANTS`
3. Добавить комментарии о безопасности для всех использований `innerHTML`
