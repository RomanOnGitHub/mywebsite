# Анализ использования Sidebar с Backlinks

## Текущая логика

**Sidebar.astro:**
- Всегда отображается на всех страницах (через Layout)
- Backlinks показывается только если передан `slug={slug}`

**Backlinks.astro:**
- Ищет связи в графе знаний по slug
- Показывает страницы, которые ссылаются на текущую страницу
- Работает только для страниц, которые есть в графе знаний (blog, cases, services, industries, legal)

---

## Анализ страниц

### ✅ **НУЖЕН Sidebar с Backlinks** (передаётся `slug`)

#### 1. Страницы статей (LayoutPost)
- **`src/pages/[lang]/blog/[...slug].astro`**
  - ✅ Использует `LayoutPost` → передаёт `slug={fullSlug}`
  - ✅ Backlinks имеет смысл: показывает, какие статьи ссылаются на текущую
  - **Slug формат:** `blog/my-article`

- **`src/pages/[lang]/cases/[...slug].astro`**
  - ✅ Использует `LayoutPost` → передаёт `slug={fullSlug}`
  - ✅ Backlinks имеет смысл: показывает связи между кейсами
  - **Slug формат:** `cases/my-case`

- **`src/pages/[lang]/services/[...slug].astro`**
  - ✅ Использует `LayoutPost` → передаёт `slug={fullSlug}`
  - ✅ Backlinks имеет смысл: показывает связи между услугами
  - **Slug формат:** `services/my-service`

- **`src/pages/[lang]/industries/[...slug].astro`**
  - ✅ Использует `LayoutPost` → передаёт `slug={fullSlug}`
  - ✅ Backlinks имеет смысл: показывает связи между отраслями
  - **Slug формат:** `industries/my-industry`

- **`src/pages/[lang]/legal/[...slug].astro`**
  - ❌ Использует `Layout` → **НЕ передаёт `slug`** (✅ ПРАВИЛЬНО!)
  - ❌ Backlinks **НЕ нужен**: legal страницы не входят в граф знаний
  - **Причина:** Legal страницы (Privacy Policy, Terms of Service) — самостоятельные документы, не связанные с контентом через граф
  - **В графе знаний:** только `['blog', 'cases', 'services', 'industries']` (см. `graph-integration.ts`)

---

### ❌ **НЕ НУЖЕН Sidebar с Backlinks** (slug не передаётся, и это правильно)

#### 2. Главные страницы
- **`src/pages/index.astro`**
  - ❌ Не передаёт `slug` → Backlinks не показывается
  - ✅ **Sidebar скрыт** (`hideSidebar={true}`): на главной странице Sidebar пустой и не нужен

- **`src/pages/[lang]/index.astro`**
  - ❌ Не передаёт `slug` → Backlinks не показывается
  - ✅ **Sidebar скрыт** (`hideSidebar={true}`): на главной странице Sidebar пустой и не нужен

#### 3. Страницы списков (index страницы)
- **`src/pages/[lang]/blog/index.astro`**
  - ❌ Не передаёт `slug` → Backlinks не показывается
  - ✅ Правильно: страница списка не имеет обратных ссылок

- **`src/pages/[lang]/cases/index.astro`**
  - ❌ Не передаёт `slug` → Backlinks не показывается
  - ✅ Правильно: страница списка не имеет обратных ссылок

- **`src/pages/[lang]/services/index.astro`**
  - ❌ Не передаёт `slug` → Backlinks не показывается
  - ✅ Правильно: страница списка не имеет обратных ссылок

- **`src/pages/[lang]/industries/index.astro`**
  - ❌ Не передаёт `slug` → Backlinks не показывается
  - ✅ Правильно: страница списка не имеет обратных ссылок

#### 4. Специальные страницы
- **`src/pages/[lang]/graph.astro`**
  - ❌ Не передаёт `slug` → Backlinks не показывается
  - ✅ Правильно: интерактивная страница графа, не имеет смысла показывать backlinks
  - **Рекомендация:** Можно скрыть Sidebar полностью на этой странице (опционально)

- **`src/pages/404.astro`**
  - ❌ Не передаёт `slug` → Backlinks не показывается
  - ✅ Правильно: страница ошибки не имеет обратных ссылок

---

## Проблемы и рекомендации

### ✅ Нет проблем

**`src/pages/[lang]/legal/[...slug].astro`**
- **Текущее состояние:** Не передаёт `slug` в Layout
- **Правильно:** Legal страницы не входят в граф знаний (см. `graph-integration.ts`, строка 22: только `['blog', 'cases', 'services', 'industries']`)
- **Причина:** Legal страницы — самостоятельные документы (Privacy Policy, Terms of Service), не связанные с контентом через граф знаний
- **Вывод:** Sidebar без Backlinks — правильное поведение для legal страниц

### 🟡 Опциональные улучшения

1. **Скрыть Sidebar на странице графа:**
   - Страница графа (`/graph`) — интерактивная визуализация
   - Sidebar занимает место и может мешать
   - Можно добавить проп `hideSidebar?: boolean` в Layout

2. **Sidebar на главных страницах:**
   - ✅ **Исправлено:** Sidebar скрыт на главных страницах (`hideSidebar={true}`)
   - Причина: На главной странице нет slug → Backlinks не показывается → Sidebar пустой и не нужен

---

## Итоговая таблица

| Страница | Layout | Slug передаётся? | Backlinks нужен? | Статус |
|----------|--------|------------------|------------------|--------|
| `[lang]/blog/[...slug]` | LayoutPost | ✅ Да | ✅ Да | ✅ Правильно |
| `[lang]/cases/[...slug]` | LayoutPost | ✅ Да | ✅ Да | ✅ Правильно |
| `[lang]/services/[...slug]` | LayoutPost | ✅ Да | ✅ Да | ✅ Правильно |
| `[lang]/industries/[...slug]` | LayoutPost | ✅ Да | ✅ Да | ✅ Правильно |
| `[lang]/legal/[...slug]` | Layout | ❌ Нет | ❌ Нет | ✅ Правильно (не в графе знаний) |
| `index` | Layout | ❌ Нет | ❌ Нет | ✅ Правильно (Sidebar скрыт) |
| `[lang]/index` | Layout | ❌ Нет | ❌ Нет | ✅ Правильно (Sidebar скрыт) |
| `[lang]/blog/index` | Layout | ❌ Нет | ❌ Нет | ✅ Правильно |
| `[lang]/cases/index` | Layout | ❌ Нет | ❌ Нет | ✅ Правильно |
| `[lang]/services/index` | Layout | ❌ Нет | ❌ Нет | ✅ Правильно |
| `[lang]/industries/index` | Layout | ❌ Нет | ❌ Нет | ✅ Правильно |
| `[lang]/graph` | Layout | ❌ Нет | ❌ Нет | ✅ Правильно (можно скрыть Sidebar) |
| `404` | Layout | ❌ Нет | ❌ Нет | ✅ Правильно |

---

## Выводы

1. **Текущая реализация полностью правильная:**
   - Backlinks показывается только на страницах статей из графа знаний (blog, cases, services, industries)
   - Не показывается на списках, главных страницах и legal страницах (где нет slug или страница не в графе)

2. **Legal страницы:**
   - Legal страницы **не входят в граф знаний** (см. `graph-integration.ts`)
   - Это самостоятельные документы (Privacy Policy, Terms of Service)
   - Sidebar без Backlinks — правильное поведение

3. **Опциональные улучшения:**
   - Можно скрыть Sidebar на странице графа (интерактивная визуализация)
   - Можно добавить контент в Sidebar для главных страниц (навигация или другая информация)
